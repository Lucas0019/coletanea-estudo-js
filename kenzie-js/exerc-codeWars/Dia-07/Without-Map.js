// Dado um array de inteiros, retorne um novo array com cada valor dobrado.
//Por exemplo: [1, 2, 3] --> [2, 4, 6]

// Para o iniciante, tente usar o método map() - ele é muito útil e é bom conhecê - lo.

function maps(x) {
    return x.map(n => n * 2);
}
