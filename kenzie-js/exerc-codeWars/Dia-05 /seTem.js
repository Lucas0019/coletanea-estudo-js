// Você receberá uma matriz a e um valor x.
// Tudo o que você precisa fazer é verificar se o array fornecido contém o valor. 
// Array pode conter números ou strings. 
// X pode ser qualquer um. 
// Retorna verdadeiro se a matriz contém o valor, falso se não contém.


check = () => {
    // your code here
    for (let i = 0; i < a.length; i++) {
        if (achou = a[i] === x) {
            return true;
        } else {
            naoAchou = a[i] != x
        }
    }
    return false
}
