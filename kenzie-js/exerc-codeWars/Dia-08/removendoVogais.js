// Crie uma função, chamada removeVowels (ou remove_vowels),
// que recebe um argumento de string e retorna a mesma string com todas as vogais removidas
// (as vogais são "a", "e", "i", "o", "u").

var removeVowels = function (str) {
    //code here
    return str.replace(/[aeiou]/gi, ''); //hehe

}